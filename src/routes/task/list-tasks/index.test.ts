import { it } from "test/utils";

describe("GET /task", () => {
  it("should list all tasks for a user", async () => {});

  it("should not list other user's tasks", async () => {});

  it("should return latest task first if sorted by createdAt", async () => {});

  it("should only return tasks with priority 1 if filtered", async () => {});
});
